{% extends 'DemoBundle::layout.html.twig' %}

{% block body -%}
    <div class="row-body content-profile">
    <h1>Demo creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('demo') }}">
            Back to the list
        </a>
    </li>
</ul>
    </div>
{% endblock %}
